# 游戏视觉识别与攻略服务对接文档

本文档描述当前 NagaAgent 已实现的最小闭环：

1. AI 触发 `game_vision` 工具调用
2. 后端截图并保存原图
3. 调用视觉模型输出三线结果（原图 / 自然语言描述 / 结构化）
4. 可选调用外部攻略服务

适用版本：当前工作分支（2026-02-12 main）

---

## 1. 总体链路

```text
用户消息
  -> 对话意图分析（conversation_analyzer_prompt）
  -> 识别为 agentType=game_vision
  -> system/background_analyzer.py 分发
  -> POST agentserver /game/screenshot_analyze
  -> screenshot + vision + optional guide bridge
  -> 结果回流 UI（clawdbot replies）
```

外部可直接调用 API Server 透传端点：`POST /game/screenshot_analyze`。

---

## 2. 端点说明

### 2.1 API Server（对外）

- 路径：`POST /game/screenshot_analyze`
- 位置：`apiserver/api_server.py`
- 作用：透传到 AgentServer 同名端点

### 2.2 AgentServer（内部执行）

- 路径：`POST /game/screenshot_analyze`
- 位置：`agentserver/agent_server.py`
- 作用：执行截图、视觉识别、可选攻略桥接

---

## 3. 请求协议

请求体（JSON）：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `query` | string | 否 | 对视觉识别/攻略的需求描述 |
| `session_id` | string | 否 | 会话 ID，用于上下文关联 |
| `include_guide` | bool | 否 | 是否调用攻略服务，默认 `true` |
| `save_screenshot` | bool | 否 | 是否保存截图，默认走配置 |
| `model` | string | 否 | 临时覆盖视觉模型名 |
| `model_url` | string | 否 | 临时覆盖视觉模型 URL |
| `api_key` | string | 否 | 临时覆盖视觉模型 API Key |

请求示例：

```json
{
  "query": "请识别当前画面并给出这一关的通关建议",
  "session_id": "session_abc123",
  "include_guide": true
}
```

---

## 4. 返回协议

统一响应：

```json
{
  "success": true,
  "result": {
    "screenshot_id": "shot_20260212_103011_1a2b3c4d",
    "screenshot_path": "/abs/path/logs/game_screenshots/2026-02-12/shot_....png",
    "timestamp": "2026-02-12T10:30:12.123456",
    "image": {
      "path": "/abs/path/logs/game_screenshots/2026-02-12/shot_....png",
      "width": 1920,
      "height": 1080
    },
    "caption": "当前为关卡准备界面，右下角可见开始行动按钮。",
    "structured": {
      "game_id": "arknights",
      "scene": "stage_prepare",
      "stage_id": "1-7",
      "confidence": 0.84,
      "ocr_texts": ["开始行动", "理智"],
      "targets": [
        {
          "name": "开始行动",
          "confidence": 0.91,
          "bbox": [1450, 870, 1790, 1010]
        }
      ]
    },
    "parse_ok": true,
    "raw_model_text": "...模型原始返回...",
    "guide": {
      "success": true,
      "status_code": 200,
      "data": {
        "answer": "建议先编入两名近卫与一名医疗..."
      }
    }
  }
}
```

说明：

- `caption`：自然语言描述（主可用结果）。
- `structured`：弱结构化，可为空对象。
- `parse_ok`：结构化是否解析成功。
- `guide`：未配置攻略服务或 `include_guide=false` 时为 `null`。

---

## 5. 配置项（复用 `computer_control`）

配置文件：`config.json`（`computer_control` 节点）

新增可用字段：

| 字段 | 默认值 | 说明 |
|---|---|---|
| `screenshot_dir` | `logs/game_screenshots` | 截图保存目录 |
| `screenshot_format` | `png` | `png/jpg/jpeg/webp` |
| `screenshot_quality` | `95` | `jpeg/webp` 生效 |
| `save_screenshot` | `true` | 是否保存截图 |
| `vision_timeout` | `45` | 视觉调用超时（秒） |
| `enable_caption` | `true` | 是否输出自然语言描述 |
| `enable_structured` | `true` | 是否输出结构化结果 |
| `guide_endpoint` | `""` | 攻略服务 URL，留空不调用 |
| `guide_timeout` | `20` | 攻略服务超时（秒） |
| `guide_api_key` | `""` | 攻略服务 Bearer Token |
| `guide_use_structured` | `true` | 调攻略时附带 `structured` |

最小配置示例：

```json
{
  "computer_control": {
    "enabled": true,
    "model": "glm-4.5v",
    "model_url": "https://open.bigmodel.cn/api/paas/v4",
    "api_key": "<YOUR_VISION_API_KEY>",
    "guide_endpoint": "http://127.0.0.1:9000/guide/analyze",
    "guide_api_key": "",
    "guide_use_structured": true
  }
}
```

---

## 6. 攻略服务对接规范（Naga -> 攻略项目）

当 `guide_endpoint` 非空且 `include_guide=true` 时，Naga 会向攻略服务发起：

- 方法：`POST`
- Header：`Content-Type: application/json`
- 若 `guide_api_key` 非空，附加：`Authorization: Bearer <guide_api_key>`

请求体：

```json
{
  "query": "请识别当前画面并给出通关建议",
  "session_id": "session_abc123",
  "screenshot_path": "/abs/path/logs/game_screenshots/2026-02-12/shot_xxx.png",
  "caption": "当前为关卡准备界面...",
  "structured": {
    "game_id": "arknights",
    "scene": "stage_prepare",
    "stage_id": "1-7",
    "confidence": 0.84,
    "ocr_texts": ["开始行动"],
    "targets": []
  }
}
```

其中：

- `structured` 仅在 `guide_use_structured=true` 时发送。
- 攻略服务建议支持 `caption` 作为主输入，`structured` 作为增强输入。

推荐响应：

```json
{
  "answer": "建议先上先锋回费，再下狙击处理飞行单位。"
}
```

兼容字段（Naga 会按顺序读取）：`answer` -> `reply` -> `text`。

---

## 7. AI 工具调用格式（意图层）

`conversation_analyzer_prompt` 已支持：

```json
{
  "agentType": "game_vision",
  "query": "截图后识别画面并告诉我下一步怎么做",
  "include_guide": true
}
```

---

## 8. 快速联调

### 8.1 直接调 API Server

```bash
curl -X POST "http://127.0.0.1:8000/game/screenshot_analyze" \
  -H "Content-Type: application/json" \
  -d '{
    "query":"识别当前画面并给攻略",
    "session_id":"debug_session_1",
    "include_guide":true
  }'
```

### 8.2 仅验证视觉（不调攻略）

```bash
curl -X POST "http://127.0.0.1:8000/game/screenshot_analyze" \
  -H "Content-Type: application/json" \
  -d '{"query":"只做画面识别","include_guide":false}'
```

---

## 9. 错误与降级策略

- 视觉模型配置缺失（model/url/api_key）：返回 500，错误信息指明缺失项。
- 截图失败：返回 500。
- 结构化解析失败：不影响主流程，`parse_ok=false`，仍返回 `caption`。
- 攻略服务不可达：不影响主流程，`guide.success=false` 且附 `error`。

---

## 10. 建议的攻略项目实现优先级

1. 先消费 `caption`，快速产出可用建议。
2. 再用 `structured.scene/game_id/targets` 做规则增强。
3. 最后再做按游戏定制的精细结构化解析。
